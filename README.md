# Hello World Python Project

A minimal Python project with a `hello.py` module and a basic pytest test.

## Setup

1. Install dependencies:

```bash
python -m pip install poetry
poetry install
```

2. Run the project:

```bash
python hello.py
```

3. Run tests:

```bash
poetry run pytest
```
